defaultNamespace: metallb-system

labels:
  app: metallb

helm:
  repo: https://metallb.github.io/metallb
  chart: metallb
  releaseName: metallb
  version: 0.16.1
  # Removed atomic: true to prevent Helm timeout/rollback on DaemonSet rollout delays
  timeoutSeconds: 600
