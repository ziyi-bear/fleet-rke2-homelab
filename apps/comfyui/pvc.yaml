apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: comfyui-data
  namespace: comfyui
  labels:
    app: comfyui
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: openebs-zfs-hdd
  resources:
    requests:
      storage: 100Gi
